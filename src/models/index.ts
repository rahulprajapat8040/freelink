import { Bids } from './bids.model';
import { Category } from './category.model';
import { DeviceInfo } from './deviceInfo.model';
import { JobsPosted } from './jobsPosted';
import { Portfolio } from './portfolio.model';
import { Reviews } from './reviews.model';
import { User } from './user.model';

export const Models = [DeviceInfo, User, JobsPosted, Bids, Category, Portfolio, Reviews];
export { DeviceInfo, User, JobsPosted, Bids, Category, Portfolio, Reviews };
