import { Bids } from './bids.model';
import { Category } from './category.model';
import { DeviceInfo } from './deviceInfo.model';
import { JobsPosted } from './jobsPosted';
import { User } from './user.model';

export const Models = [DeviceInfo, User, JobsPosted, Bids, Category];
export { DeviceInfo, User, JobsPosted, Bids, Category };
